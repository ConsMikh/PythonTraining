name = 'albert einstein'
quote = ' E = mc**2  '
quotions = '{} once said, {}'.format(name.title(), quote.strip())
print(quotions)

quotions = f'{name.title()} once said, {quote.strip()}'
print(quotions)
